apiVersion: apps/v1
kind: Deployment
metadata:
  name: loruceanro-catalog-service
  labels:
    app: loruceanro-catalog-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loruceanro-catalog-service
  template:
    metadata:
      labels:
        app: loruceanro-catalog-service
    spec:
      initContainers:
        - name: wait-for-db
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - until pg_isready -h loruceanro-postgres -U beatplugg -d postgres; do sleep 2; done
      containers:
        - name: loruceanro-catalog-service
          image: beatplugg/loruceanro-catalog-service
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: catalog-db-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: redis-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: jwt-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: loruceanro-catalog-service
spec:
  selector:
    app: loruceanro-catalog-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
