apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/loruceanro
  - sealed-secret.yaml

patches:
  - path: patches/pvc-storage.yaml
  - path: patches/ingress.yaml
  - path: patches/frontend-svc.yaml
